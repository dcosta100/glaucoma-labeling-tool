# ─────────────────────────────────────────────────────────────────────────────
# Labeler configuration (one per machine)
# ─────────────────────────────────────────────────────────────────────────────
# Name of the person grading on this machine. It is stored together with the
# labels so we can consolidate everyone's work later. You can also set it the
# first time you open the app (it will ask and save it here automatically).
labeler_name: "doug"

# Paths (usually no need to change). Relative to the project folder.
manifest_path: data/prepared/manifest.csv
images_dir: data/prepared/images
output_dir: labels
reference_guide: reference/Visual_Field_Patterns_Revised.docx
